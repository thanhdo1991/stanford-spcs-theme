<video width="{{ video.width }}" height="{{ video.height }}" muted="{{ video.muted }}" loop="{{ video.loop }}" autoplay="{{ video.autoplay }}" preload="{{ video.preload }}">
  <source src="{{ video.src }}" type="{{ video.type }}">
</video>
